/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['cdn-icons-png.flaticon.com' , 'storage.googleapis.com'], // Add other domains as needed
  },
};
export default nextConfig;
